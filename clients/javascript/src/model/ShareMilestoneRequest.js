/**
 * Gamified Task Management API
 * API REST gamificada para gerenciamento de tarefas com IA, XP, levels e sistema de recompensas
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ShareMilestoneRequestData from './ShareMilestoneRequestData';

/**
 * The ShareMilestoneRequest model module.
 * @module model/ShareMilestoneRequest
 * @version 1.0.0
 */
class ShareMilestoneRequest {
    /**
     * Constructs a new <code>ShareMilestoneRequest</code>.
     * @alias module:model/ShareMilestoneRequest
     * @param type {module:model/ShareMilestoneRequest.TypeEnum} 
     */
    constructor(type) { 
        
        ShareMilestoneRequest.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
        obj['type'] = type;
    }

    /**
     * Constructs a <code>ShareMilestoneRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ShareMilestoneRequest} obj Optional instance to populate.
     * @return {module:model/ShareMilestoneRequest} The populated <code>ShareMilestoneRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ShareMilestoneRequest();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('data')) {
                obj['data'] = ShareMilestoneRequestData.constructFromObject(data['data']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ShareMilestoneRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ShareMilestoneRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ShareMilestoneRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // validate the optional field `data`
        if (data['data']) { // data not null
          ShareMilestoneRequestData.validateJSON(data['data']);
        }

        return true;
    }


}

ShareMilestoneRequest.RequiredProperties = ["type"];

/**
 * @member {module:model/ShareMilestoneRequest.TypeEnum} type
 */
ShareMilestoneRequest.prototype['type'] = undefined;

/**
 * @member {module:model/ShareMilestoneRequestData} data
 */
ShareMilestoneRequest.prototype['data'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ShareMilestoneRequest['TypeEnum'] = {

    /**
     * value: "level_up"
     * @const
     */
    "level_up": "level_up",

    /**
     * value: "badge_earned"
     * @const
     */
    "badge_earned": "badge_earned",

    /**
     * value: "streak_milestone"
     * @const
     */
    "streak_milestone": "streak_milestone",

    /**
     * value: "custom"
     * @const
     */
    "custom": "custom"
};



export default ShareMilestoneRequest;

